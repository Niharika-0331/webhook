pipeline{
    agent any

    stages {
        stage('Git Checkout') {
            steps {
          echo 'Git checkout'
}
}
 stage('build') {
            steps {
          echo 'building the binary'
}
}
 stage('deploying the binary') {
            steps {
          echo 'deploying the binary'
}
}
}
}
